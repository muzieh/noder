import patataj from './patat';

export type Person = {
  name: string;
  age: number;
  city: string;
}
const abc: number = 123;

console.log(abc);

const per: Person = {
  name: "John",
  age: 30,
  city: "New York"
}

console.log(per);
console.log(patataj());
